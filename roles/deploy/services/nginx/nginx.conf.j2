worker_processes 2;

events { worker_connections 1024; }

http {
    server {
        listen 9000;
        server_name localhost;
        location / {
        	proxy_pass http://auth-server:9000;
        }
    }
    server {
    	    listen 8080;
    		server_name localhost;
    		location / {
    			proxy_pass http://auth-client:8080;
    		}
    	}
}